# 对称二叉树（Symmetric Tree）

- 题目链接：https://leetcode.cn/problems/symmetric-tree/
- 难度：简单
- 标签：树、深度优先搜索、广度优先搜索、二叉树

## 题目描述

给你一个二叉树的根节点 `root`，检查它是否**轴对称**。

### 示例

```
输入：root = [1,2,2,3,4,4,3]
输出：true

输入：root = [1,2,2,null,3,null,3]
输出：false
```

### 约束

- 树中节点数目在范围 `[1, 1000]` 内
- `-100 <= Node.val <= 100`

---

## 解法一：递归比较镜像节点（推荐，同 solution.py）

### 思路

对称等价于：左子树的左与右子树的右镜像，左子树的右与右子树的左镜像。递归比较两棵「镜像子树」。

### 代码

```python
from typing import Optional


class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


class Solution:
    def isSymmetric(self, root: Optional[TreeNode]) -> bool:
        def mirror(a: Optional[TreeNode], b: Optional[TreeNode]) -> bool:
            if not a and not b:
                return True
            if not a or not b or a.val != b.val:
                return False
            return mirror(a.left, b.right) and mirror(a.right, b.left)

        return mirror(root, root) if root else True
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(n) |
| 空间 | O(h) |

### 讲解

将「整棵树对称」转化为「两个指针镜像 walk」。注意比较的是 `(left.left, right.right)` 与 `(left.right, right.left)`。

---

## 解法二：BFS 成对入队

### 思路

队列中同时存放镜像节点对 `(u, v)`，每次弹出比较，并按镜像关系将子节点成对入队。

### 代码

```python
from collections import deque


class Solution:
    def isSymmetric(self, root: Optional[TreeNode]) -> bool:
        if not root:
            return True
        q = deque([(root.left, root.right)])
        while q:
            a, b = q.popleft()
            if not a and not b:
                continue
            if not a or not b or a.val != b.val:
                return False
            q.append((a.left, b.right))
            q.append((a.right, b.left))
        return True
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(n) |
| 空间 | O(w) |

### 讲解

BFS 版本避免递归，逻辑与递归完全对应。`continue` 处理双 null 的情况。

---

## 解法三：中序遍历辅助（理解用）

### 思路

对称树的中序遍历若把 null 也输出占位，结果应是回文。可用双指针检查序列（需统一 null 占位规则）。

### 代码

```python
class Solution:
    def isSymmetric(self, root: Optional[TreeNode]) -> bool:
        def inorder(node: Optional[TreeNode]) -> list:
            if not node:
                return ["#"]
            return inorder(node.left) + [node.val] + inorder(node.right)

        seq = inorder(root)
        return seq == seq[::-1]
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(n) |
| 空间 | O(n) |

### 讲解

此方法依赖 null 占位规则，不如直接镜像比较稳健，主要用于理解「对称 = 序列回文」的直觉。

---

## 解法对比

| 解法 | 时间 | 空间 | 适用场景 |
|------|------|------|----------|
| 递归镜像 | O(n) | O(h) | 面试首选 |
| BFS 成对 | O(n) | O(w) | 迭代实现 |
| 中序回文 | O(n) | O(n) | 理解对称本质 |

---

## 常见错误

1. **只比较根左右值**：还需递归比较子树镜像关系。
2. **比较 (left.left, right.left)**：应为 `(left.left, right.right)`。
3. **空树**：题目约束 n≥1，但代码中 `root is None` 应返回 True。

---

## 举一反三

- **翻转二叉树**（226）：构造镜像树
- **相同的树**（100）：同向比较而非镜像
- **对称字符串**（双指针）：序列层面的对称
