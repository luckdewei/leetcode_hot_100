# 二叉树的最大深度（Maximum Depth of Binary Tree）

- 题目链接：https://leetcode.cn/problems/maximum-depth-of-binary-tree/
- 难度：简单
- 标签：树、深度优先搜索、广度优先搜索、二叉树

## 题目描述

给定一个二叉树，找出其**最大深度**。

二叉树的深度为根节点到最远叶子节点的最长路径上的节点数。

### 示例

```
输入：root = [3,9,20,null,null,15,7]
输出：3

输入：root = [1,null,2]
输出：2
```

### 约束

- 节点数量在 `[0, 10^4]` 范围内
- `-100 <= Node.val <= 100`

---

## 解法一：递归 DFS（推荐，同 solution.py）

### 思路

空树深度为 0；否则深度 = 1 + max(左子树深度, 右子树深度)。

### 代码

```python
from typing import Optional


class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        return 1 + max(self.maxDepth(root.left), self.maxDepth(root.right))
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(n) |
| 空间 | O(h)，递归栈 |

### 讲解

这是树形 DP 的经典模板：子问题（左右子树深度）合并得到当前答案。代码极简，面试首选。

---

## 解法二：BFS 层序遍历

### 思路

按层扩展节点，层数即为深度。每轮处理当前队列中全部节点，再进入下一层。

### 代码

```python
from collections import deque


class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        depth, q = 0, deque([root])
        while q:
            depth += 1
            for _ in range(len(q)):
                node = q.popleft()
                if node.left:
                    q.append(node.left)
                if node.right:
                    q.append(node.right)
        return depth
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(n) |
| 空间 | O(w)，w 为树的最大宽度 |

### 讲解

BFS 直观体现「一层一层数深度」。宽树时队列空间可能大于递归栈。

---

## 解法三：迭代 DFS + 栈

### 思路

用栈保存 `(node, depth)`，弹出时更新最大深度，并将子节点压栈。

### 代码

```python
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        ans, stack = 0, [(root, 1)]
        while stack:
            node, d = stack.pop()
            ans = max(ans, d)
            if node.right:
                stack.append((node.right, d + 1))
            if node.left:
                stack.append((node.left, d + 1))
        return ans
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(n) |
| 空间 | O(h) |

### 讲解

避免递归栈溢出时的替代方案，逻辑与递归等价。

---

## 解法对比

| 解法 | 时间 | 空间 | 适用场景 |
|------|------|------|----------|
| 递归 DFS | O(n) | O(h) | 简洁，面试首选 |
| BFS | O(n) | O(w) | 需要层信息时 |
| 迭代 DFS | O(n) | O(h) | 深树防栈溢出 |

---

## 常见错误

1. **深度与高度混淆**：本题从根向下数，叶子深度最大；有些题从叶子向上定义高度。
2. **空树返回 1**：空树深度应为 0。
3. **只递归一边**：必须取左右最大值。

---

## 举一反三

- **二叉树的最小深度**（111）：需区分「只有左或只有右」的情况
- **平衡二叉树**（110）：比较左右子树高度差
- **二叉树的直径**（543）：深度思想的延伸
