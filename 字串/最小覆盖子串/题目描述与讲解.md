# 最小覆盖子串（Minimum Window Substring）

- 题目链接：https://leetcode.cn/problems/minimum-window-substring/
- 难度：困难
- 标签：哈希表、字符串、滑动窗口

## 题目描述

给你一个字符串 `s`、一个字符串 `t`。返回 `s` 中涵盖 `t` 所有字符的最小子串。如果 `s` 中不存在涵盖 `t` 所有字符的子串，返回空字符串 `""`。

### 示例

```
输入：s = "ADOBECODEBANC", t = "ABC"
输出："BANC"

输入：s = "a", t = "a"
输出："a"

输入：s = "a", t = "aa"
输出：""
```

### 约束

- `m == s.length`, `n == t.length`
- `1 <= m, n <= 10^5`
- `s` 和 `t` 由英文字母组成

---

## 解法一：可变长滑动窗口（推荐）

### 思路

右指针扩展窗口，当窗口已包含 `t` 全部字符时，左指针收缩求最短。用 `formed` 计数已满足要求的字符种类数。

### 代码

```python
from collections import Counter


class Solution:
    def minWindow(self, s: str, t: str) -> str:
        if not t or not s:
            return ""
        need = Counter(t)
        window = Counter()
        required = len(need)
        formed = 0
        left = 0
        best_len = float("inf")
        best_left = 0
        for right, ch in enumerate(s):
            window[ch] += 1
            if ch in need and window[ch] == need[ch]:
                formed += 1
            while formed == required:
                if right - left + 1 < best_len:
                    best_len = right - left + 1
                    best_left = left
                left_ch = s[left]
                window[left_ch] -= 1
                if left_ch in need and window[left_ch] < need[left_ch]:
                    formed -= 1
                left += 1
        return "" if best_len == float("inf") else s[best_left:best_left + best_len]
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(\|s\| + \|t\|) |
| 空间 | O(\|s\| + \|t\|) |

### 讲解

左右指针各最多移动 n 次。`formed == required` 表示 t 中每种字符频次都已满足。

---

## 解法二：固定模板 + 缺失计数

### 思路

维护 `missing` 为还需匹配的字符总数。右扩时若字符有用则 `missing--`；`missing==0` 时左缩更新答案。

### 代码

```python
from collections import Counter


class Solution:
    def minWindow(self, s: str, t: str) -> str:
        need = Counter(t)
        missing = len(t)
        left = 0
        start, length = 0, float("inf")
        for right, ch in enumerate(s):
            if need[ch] > 0:
                missing -= 1
            need[ch] -= 1
            while missing == 0:
                if right - left + 1 < length:
                    start, length = left, right - left + 1
                need[s[left]] += 1
                if need[s[left]] > 0:
                    missing += 1
                left += 1
        return "" if length == float("inf") else s[start:start + length]
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(\|s\| + \|t\|) |
| 空间 | O(\|t\|) |

### 讲解

用 `missing` 替代 `formed`，逻辑等价但更紧凑。注意 `need[ch]` 可为负表示多余字符。

---

## 解法三：暴力枚举（理解用）

### 思路

枚举所有子串，检查是否包含 t 全部字符。

### 代码

```python
from collections import Counter


class Solution:
    def minWindow(self, s: str, t: str) -> str:
        need = Counter(t)
        best = ""
        for i in range(len(s)):
            cnt = Counter()
            formed = 0
            for j in range(i, len(s)):
                ch = s[j]
                cnt[ch] += 1
                if ch in need and cnt[ch] == need[ch]:
                    formed += 1
                if formed == len(need):
                    sub = s[i:j + 1]
                    if not best or len(sub) < len(best):
                        best = sub
                    break
        return best
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(n²) 或 O(n² × \|t\|) |
| 空间 | O(\|t\|) |

### 讲解

O(n²) 会超时，帮助理解「覆盖」的含义。

---

## 解法对比

| 解法 | 时间 | 空间 | 适用场景 |
|------|------|------|----------|
| 滑动窗口 | O(n) | O(k) | 面试首选 |
| missing 计数 | O(n) | O(k) | 写法紧凑 |
| 暴力 | O(n²) | O(k) | 理解用 |

---

## 常见错误

1. **用 `>=` 判断字符满足**：应精确到 `== need[ch]` 才增加 formed。
2. **收缩时漏减 formed**：左移导致某字符不足时要减 formed。
3. **与「找到所有异位词」混淆**：438 题固定窗口长，本题可变长。

---

## 举一反三

- **字符串的排列**（567）：固定长滑动窗口
- **找到字符串中所有字母异位词**（438）
- **替换后的最长重复字符**（424）
