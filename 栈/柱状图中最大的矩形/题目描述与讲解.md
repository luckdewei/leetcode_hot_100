# 柱状图中最大的矩形（Largest Rectangle in Histogram）

- 题目链接：https://leetcode.cn/problems/largest-rectangle-in-histogram/
- 难度：困难
- 标签：栈、数组、单调栈

## 题目描述

给定 n 个非负整数表示柱状图各柱子高度，每根柱子宽度为 1。找出能勾勒出的最大矩形面积。

### 示例

```
输入：heights = [2,1,5,6,2,3]
输出：10
解释：高度 5 和 6 的柱子组成宽 2 的矩形，面积 10。

输入：heights = [2,4]
输出：4
```

### 约束

- `1 <= heights.length <= 10^5`
- `0 <= heights[i] <= 10^4`

---

## 解法一：单调递增栈 + 哨兵（推荐）

### 思路

维护单调递增栈存下标。当前高度小于栈顶高度时，栈顶即「以该高度为高的矩形」的右边界，弹出并计算面积。两侧加 0 哨兵统一处理边界。

### 代码

```python
from typing import List


class Solution:
    def largestRectangleArea(self, heights: List[int]) -> int:
        heights = [0] + heights + [0]
        stack = [0]
        ans = 0
        for i in range(1, len(heights)):
            while heights[i] < heights[stack[-1]]:
                h_idx = stack.pop()
                h = heights[h_idx]
                w = i - stack[-1] - 1
                ans = max(ans, h * w)
            stack.append(i)
        return ans
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(n) |
| 空间 | O(n) |

### 讲解

每个下标最多入栈出栈一次。宽度 = 当前下标 - 弹出后栈顶 - 1。哨兵 0 保证所有柱子最终被处理。

---

## 解法二：单调栈（无哨兵）

### 思路

遍历结束后若栈非空，依次弹出计算；或在 heights 末尾追加 0 触发剩余弹出。

### 代码

```python
from typing import List


class Solution:
    def largestRectangleArea(self, heights: List[int]) -> int:
        stack = []
        ans = 0
        heights = heights + [0]
        for i, h in enumerate(heights):
            while stack and heights[stack[-1]] > h:
                idx = stack.pop()
                width = i if not stack else i - stack[-1] - 1
                ans = max(ans, heights[idx] * width)
            stack.append(i)
        return ans
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(n) |
| 空间 | O(n) |

### 讲解

末尾加 0 等价于右哨兵。弹出时宽度由栈顶下标与当前 i 决定。

---

## 解法三：暴力枚举（理解用）

### 思路

以每根柱子为高度，向左右扩展直到遇到更矮柱子，计算面积。

### 代码

```python
from typing import List


class Solution:
    def largestRectangleArea(self, heights: List[int]) -> int:
        n = len(heights)
        ans = 0
        for i in range(n):
            left, right = i, i
            while left > 0 and heights[left - 1] >= heights[i]:
                left -= 1
            while right < n - 1 and heights[right + 1] >= heights[i]:
                right += 1
            ans = max(ans, heights[i] * (right - left + 1))
        return ans
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(n²) |
| 空间 | O(1) |

### 讲解

O(n²) 会超时，帮助理解「以 i 为高的最大宽度」含义。

---

## 解法对比

| 解法 | 时间 | 空间 | 适用场景 |
|------|------|------|----------|
| 单调栈+哨兵 | O(n) | O(n) | 面试首选 |
| 末尾加 0 | O(n) | O(n) | 等价写法 |
| 暴力 | O(n²) | O(1) | 理解用 |

---

## 常见错误

1. **宽度计算错**：应为 `i - stack[-1] - 1`，不是 `i - h_idx`。
2. **栈存值而非下标**：需下标算宽度。
3. **与「接雨水」混淆**：接雨水用单调递减栈，本题递增栈。

---

## 举一反三

- **最大矩形**（85）：二维化为一维柱状图
- **每日温度**（739）：单调递减栈
- **接雨水**（42）：双指针 / 单调栈
