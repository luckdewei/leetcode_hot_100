# 合并 K 个升序链表（Merge k Sorted Lists）

- 题目链接：https://leetcode.cn/problems/merge-k-sorted-lists/
- 难度：困难
- 标签：链表、分治、堆（优先队列）、归并排序

## 题目描述

给你一个链表数组，每个链表已按升序排列。请将所有链表合并到一个升序链表中并返回。

### 示例

```
输入：lists = [[1,4,5],[1,3,4],[2,6]]
输出：[1,1,2,3,4,4,5,6]

输入：lists = []
输出：[]

输入：lists = [[]]
输出：[]
```

### 约束

- `k == lists.length`
- `0 <= k <= 10^4`
- `0 <= lists[i].length <= 500`
- `-10^4 <= lists[i][j] <= 10^4`
- `lists[i]` 升序排列
- `lists[i].length` 总和 `<= 10^4`

---

## 解法一：最小堆（推荐）

### 思路

将每个链表头节点入最小堆。每次弹出最小节点接到结果，若该节点有 next 则入堆。

### 代码

```python
import heapq
from typing import List, Optional


class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next


class Solution:
    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:
        heap = []
        for i, node in enumerate(lists):
            if node:
                heapq.heappush(heap, (node.val, i, node))
        dummy = ListNode(0)
        cur = dummy
        while heap:
            val, i, node = heapq.heappop(heap)
            cur.next = node
            cur = cur.next
            if node.next:
                heapq.heappush(heap, (node.next.val, i, node.next))
        return dummy.next
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(N log k)，N 为总节点数 |
| 空间 | O(k) |

### 讲解

堆中最多 k 个节点。tuple 中 i 避免 val 相等时比较 ListNode 报错。

---

## 解法二：分治归并

### 思路

两两合并链表，类似归并排序。`mergeTwoLists` 合并两个有序链表。

### 代码

```python
from typing import List, Optional


class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next


class Solution:
    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:
        if not lists:
            return None
        while len(lists) > 1:
            merged = []
            for i in range(0, len(lists), 2):
                l1 = lists[i]
                l2 = lists[i + 1] if i + 1 < len(lists) else None
                merged.append(self._merge(l1, l2))
            lists = merged
        return lists[0]

    def _merge(self, l1, l2):
        dummy = ListNode(0)
        cur = dummy
        while l1 and l2:
            if l1.val <= l2.val:
                cur.next = l1
                l1 = l1.next
            else:
                cur.next = l2
                l2 = l2.next
            cur = cur.next
        cur.next = l1 if l1 else l2
        return dummy.next
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(N log k) |
| 空间 | O(1) 或 O(log k) |

### 讲解

分治每层合并全部链表，总时间 O(N log k)。无堆依赖，同样优秀。

---

## 解法三：顺序合并

### 思路

依次将 lists[i] 合并到结果链表。

### 代码

```python
from typing import List, Optional


class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next


class Solution:
    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:
        ans = None
        for node in lists:
            ans = self._merge(ans, node)
        return ans

    def _merge(self, l1, l2):
        dummy = ListNode(0)
        cur = dummy
        while l1 and l2:
            if l1.val <= l2.val:
                cur.next = l1
                l1 = l1.next
            else:
                cur.next = l2
                l2 = l2.next
            cur = cur.next
        cur.next = l1 if l1 else l2
        return dummy.next
```

### 复杂度

| 指标 | 复杂度 |
|------|--------|
| 时间 | O(kN) 最坏 |
| 空间 | O(1) |

### 讲解

顺序合并可能 O(kN)，链表长度不均时效率差。

---

## 解法对比

| 解法 | 时间 | 空间 | 适用场景 |
|------|------|------|----------|
| 最小堆 | O(N log k) | O(k) | 面试首选 |
| 分治 | O(N log k) | O(1) | 无堆环境 |
| 顺序合并 | O(kN) | O(1) | 理解用 |

---

## 常见错误

1. **堆元素只有 val 无 index**：val 相同时比较 Node 报错。
2. **忘记过滤空链表**：入堆前检查 `if node`。
3. **与「合并两个有序链表」混淆**：23 题是两链表特例。

---

## 举一反三

- **合并两个有序链表**（23）
- **排序链表**（148）：归并排序
- **查找和最小的 K 对数字**（373）
